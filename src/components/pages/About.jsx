import React from 'react'

export const About = () => {
  return (   
     <><section className='about'>
      <div className="container flex">
      {about.map((val,i)=>(

      ))}
      </div>
   </section></>

  )
  
}

